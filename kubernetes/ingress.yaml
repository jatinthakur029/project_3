apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: tetris-ingress
spec:
  ingressClassName: nginx
  rules:
  - http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: tetris-service
            port:
              number: 80
      - path: /api
        pathType: Prefix
        backend:
          service:
            name: tetrisbac-service
            port:
              number: 3000          
 