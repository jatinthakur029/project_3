apiVersion: v1
kind: Service
metadata:
  name: tetrisBac-service
spec:
  type: NodePort
  selector:
    app: tetrisBac
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000
      nodePort: 30101 